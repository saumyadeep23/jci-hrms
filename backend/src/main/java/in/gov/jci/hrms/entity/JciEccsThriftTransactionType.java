package in.gov.jci.hrms.entity;

/** jcieccs_thrift_transaction.transaction_type - DB-enforced via chk_jcieccs_th_type. */
public enum JciEccsThriftTransactionType {
    CONTRIBUTION,
    ADJUSTMENT,
    REFUND,
    OPENING_BALANCE
}
