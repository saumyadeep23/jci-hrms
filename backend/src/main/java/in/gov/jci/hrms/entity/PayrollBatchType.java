package in.gov.jci.hrms.entity;

/** payroll_batches.batch_type (V78) - REGULAR is the ordinary monthly cycle; SUPPLEMENTARY covers an off-cycle correction run for the same or an earlier month. */
public enum PayrollBatchType {
    REGULAR,
    SUPPLEMENTARY
}
