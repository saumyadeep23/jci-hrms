package in.gov.jci.hrms.entity;

/** cpf_annual_interest_runs.scope - whether a run covers every member with ledger activity, or one member's correction/recalculation. */
public enum CpfInterestRunScope {
    ALL_MEMBERS,
    SELECTED_MEMBER
}
