package in.gov.jci.hrms.entity;

/** employee_vehicle_allotments.status - matches its DB CHECK constraint exactly. */
public enum VehicleAllotmentStatus {
    ACTIVE,
    SURRENDERED,
    TRANSFERRED,
    CANCELLED
}
