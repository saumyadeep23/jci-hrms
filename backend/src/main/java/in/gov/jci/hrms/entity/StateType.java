package in.gov.jci.hrms.entity;

/** NATIONAL_CAPITAL_TERRITORY exists solely for Delhi ("NCT" in state_master.state_type) - constitutionally distinct from a plain Union Territory. */
public enum StateType {
    STATE,
    UNION_TERRITORY,
    NATIONAL_CAPITAL_TERRITORY
}
