package in.gov.jci.hrms.entity;

/** payroll_statutory_parameters.val_type - matches its DB CHECK constraint exactly. */
public enum StatutoryParamValueType {
    DECIMAL,
    PERCENTAGE,
    AMOUNT
}
