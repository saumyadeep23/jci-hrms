# JCI HRMS — VAPT Readiness Register

Baseline commit `6067227b40c679d9be83ad1f709e818d93405b71`. This register lists every significant control reviewed during the pre-RBAC audit. Full narrative evidence for each finding ID is in `PRE_RBAC_SECURITY_AUDIT.md`.

Remediation Phase legend: **BLOCK_BEFORE_RBAC**, **INCORPORATE_IN_RBAC**, **FIX_BEFORE_PRODUCTION**, **HARDEN_BEFORE_FORMAL_VAPT**, **INFRA_VERIFICATION_REQUIRED**.

| ID | Domain | Control | Framework Mapping | Status | Severity | Evidence | Risk | Recommendation | Remediation Phase | Verification Method |
|---|---|---|---|---|---|---|---|---|---|---|
| SEC-001 | Authentication | Production must not start with a forgeable/default JWT signing key | OWASP A07:2021 (Identification & Auth Failures), API Security Top 10 API2 (Broken Authentication), CWE-798 (Hardcoded Credentials), CWE-321 (Hardcoded Crypto Key) | **COMPLIANT** (remediated 2026-09-15, see `SEC_001_002_REMEDIATION.md`) | CRITICAL (closed) | `SecurityConfig.jwtDecoder()` now requires an explicit `security.auth.mode` (`oidc`/`local-dev`) and calls the new `AuthenticationModeGuard.validate(...)` (`AuthenticationModeGuard.java`, `JwtAuthMode.java`) before building any decoder — throws `IllegalStateException` (aborting startup) whenever a production-like Spring profile (`staging`/`prod`, mirroring `infra/main.tf`'s `local.hardened`) would reach local-dev HS256 auth, a missing issuer, or the still-default local-dev secret. `infra/main.tf`/`variables.tf` now pass `SPRING_PROFILES_ACTIVE`, `JWT_AUTH_MODE`, `JWT_ISSUER_URI` into the ECS task definition. | Was: full auth bypass via a forgeable default HS256 secret. Now: a production-like deploy without correct OIDC configuration fails to start rather than silently accepting local-dev auth. | Remediated. Carried forward to RBAC phase: audience validation still requires a real IdP client-id (not yet provisioned) — see `RBAC_SECURITY_REQUIREMENTS.md` §10. | — (closed) | `AuthenticationModeGuardTest`, `SecurityConfigJwtDecoderTest`, `JwtAuthenticationSecurityTest` (48 tests total across the SEC-001 suite incl. pre-existing `SecurityConfigTest`, all passing) |
| SEC-002 | Authorization (BOLA) | Attendance-punch creation must be scoped to the caller or an authorized role | OWASP API1:2023 (BOLA), CWE-639 (Authorization Bypass Through User-Controlled Key) | **COMPLIANT** (remediated 2026-09-15, see `SEC_001_002_REMEDIATION.md`) | CRITICAL (closed) | `MobilePunchController.create()` is now gated `@PreAuthorize("@attendanceAggSec.canEvaluateFor(authentication, #request.employeeId())")` (self, or HR_ADMIN acting on another's behalf — reusing the existing `AttendanceAggregationSecurity` bean already used by `AttendanceAggregationController`/`LeaveLedgerEntryController`; SUPER_ADMIN removed from this grant 2026-09-16, see `RBAC_MIGRATION_REPORT.md`). `MobilePunchService.create(request, callerEmployeeId, onBehalfOfOthersPermitted)` independently re-derives and re-checks the same rule (throws `AccessDeniedException` on mismatch) as a service-layer defense-in-depth backstop. | Was: any authenticated user, any role, could fabricate another employee's time+geolocation punch. Now: only the caller's own identity (JWT-derived) or an explicit HR_ADMIN override is accepted, enforced at both controller and service layers. | Remediated. SEC-005/SEC-006/SEC-007 share the same root-cause pattern in separate controllers/services and remain open — not fixed by this change, tracked separately (see rows below and `RBAC_SECURITY_REQUIREMENTS.md` §6). | — (closed) | `MobilePunchControllerTest` (15 tests, incl. explicit self-allowed/cross-employee-denied/privileged-allowed cases with `verifyNoInteractions` proof-of-no-record), `MobilePunchServiceTest` (18 tests), `AttendanceAggregationSecurityTest` (new, 11 tests) — all passing |
| SEC-003 | Financial / Maker-Checker | CPF loan apply/sanction/disburse/reject/settle must require different actors | OWASP API5:2023 (Broken Function-Level Authorization), CWE-841 (Improper Enforcement of Behavioral Workflow) | **COMPLIANT** (remediated 2026-09-15, settle-cash + legacy SUPER_ADMIN bypass closed 2026-09-16, see `MAKER_CHECKER_IMPLEMENTATION.md` / `RBAC_MIGRATION_REPORT.md`) | CRITICAL (closed) | `CpfLoanApplicationService.sanctionLoan()`/`disburseLoan()` and `CpfLoanSettlementService.processCashSettlement()` all now reject when the acting officer equals the persisted `applicantEmployeeId` (V96 migration adds the actor columns). `CpfLoanController`'s `sanction`/`disburse`/`reject`/`settle-cash` and `CpfApplicationController`'s `sanction`/`disburse`/`reject` no longer grant the legacy `SUPER_ADMIN` role — narrowed to `FINANCE_ADMIN`/`CPF_ADMIN` only, so the legacy authorization model can no longer bypass the same boundary. | Was: any role-holder (including a `SUPER_ADMIN`-only caller) could apply, sanction, disburse, and cash-settle their own loan. Now: all four transitions reject same-actor regardless of role, and `SUPER_ADMIN` alone cannot reach any of them. | Full DB-backed `FIN_MAKER_CPF`/`FIN_ADMIN_CPF` permission migration on `CpfLoanController` still not done this phase (still legacy-role-gated, just without the `SUPER_ADMIN` bypass) — see `RBAC_MIGRATION_REPORT.md`. | — (closed for apply/sanction/disburse/settle-cash) | `CpfLoanApplicationServiceTest` (4 tests), `CpfLoanTwoPhaseAndSettlementTest` (+2 settle-cash tests), `CpfLoanControllerSecurityTest` (new — SUPER_ADMIN-only 403 on all 4 endpoints, CPF_ADMIN not forbidden) |
| SEC-004 | Financial / Maker-Checker | JCIECCS recovery creation and reversal must require different actors | Same as SEC-003 | **COMPLIANT** (remediated 2026-09-15; concurrency-test residue root-caused and fixed 2026-09-16) | CRITICAL (closed) | `JciEccsRecoveryService.reverseRecovery()` now throws `AccessDeniedException` when `performedByEmployeeId` equals `original.getCreatedBy()`. No migration needed — `created_by` already existed. Phase 5 locking/idempotency untouched and re-confirmed correct by live diagnostic tracing during the 2026-09-16 closure (see below) — no production concurrency defect found. `reverseRecovery` itself still has no REST endpoint in this codebase, so there is no live HTTP path to it yet (tracked, not a regression). | Was: single role-holder could create and reverse the same recovery. Now: rejected when same actor. | Same-actor check applied to reversal only (the primary SEC-004 finding); recovery creation itself (`createAndPostPayrollRecovery`/cash) has no separate maker/checker split since it's the "maker" step by definition. `JciEccsReconciliationController.resolve`/`JciEccsSettlementController.clear` (the JCIECCS financial-approval actions actually reachable via HTTP) had their `SUPER_ADMIN` grant removed 2026-09-16 — see `RBAC_MIGRATION_REPORT.md`. | — (closed) | `JciEccsRecoveryServiceTest`, `JciEccsFinancialInvariantSuiteTest`, `JciEccsReconciliationServiceTest`, `JciEccsConcurrencyHardeningTest` regression suite — **106/106, 0 failures** (previously-failing `scenarioD2` was test-residue, not a production defect — see `MAKER_CHECKER_IMPLEMENTATION.md`) |
| SEC-005 | Authorization (BOLA) | Attendance regularization submission must be scoped to the caller | OWASP API1:2023, CWE-639 | **COMPLIANT** (remediated 2026-09-15) | HIGH (closed) | `AttendanceRegularizationController.submit()` now gated `@PreAuthorize("@attendanceAggSec.canEvaluateFor(authentication, #request.employeeId())")`; `AttendanceRegularizationService.submit()` re-checks via `callerEmployeeId`/`onBehalfOfOthersPermitted`, same pattern as SEC-002. | Was: any EMPLOYEE role could submit for another employee. Now: only self or HR_ADMIN (SUPER_ADMIN removed from the shared canActOnBehalfOfOthers grant 2026-09-16 - see RBAC_MIGRATION_REPORT.md). | — | — (closed) | `AttendanceRegularizationServiceTest` (2 new tests) |
| SEC-006 | Authorization (BOLA) / Financial | Leave-encashment application must be scoped to the caller | OWASP API1:2023, CWE-639 | **COMPLIANT** (remediated 2026-09-15) | HIGH (closed) | `LeaveEncashmentController.apply()` same pattern as SEC-005; `LeaveEncashmentService.apply()` re-checks ownership. | Was: any EMPLOYEE could file an encashment claim against another employee's balance. Now: self or HR_ADMIN only (SUPER_ADMIN removed 2026-09-16 - see RBAC_MIGRATION_REPORT.md). | — | — (closed) | `LeaveEncashmentServiceTest` (1 new test) |
| SEC-007 | Authorization (BOLA) | Leave-application creation must be scoped to the caller | OWASP API1:2023, CWE-639 | **COMPLIANT** (remediated 2026-09-15) | MEDIUM (closed) | `LeaveApplicationController.create()` same pattern, overriding the class-level role-only `@PreAuthorize`; `LeaveApplicationService.create()` re-checks ownership. | Was: contained nuisance risk (DRAFT pollution). Now: closed entirely — creation itself requires self or HR_ADMIN (SUPER_ADMIN removed 2026-09-16 - see RBAC_MIGRATION_REPORT.md). | — | — (closed) | `LeaveApplicationServiceTest`, `LeaveApplicationControllerTest` (new tests) |
| SEC-008 | Financial / Audit Trail | CPF loan lifecycle must record actor identity on every state change | OWASP A09:2021 (Security Logging & Monitoring Failures), CWE-778 (Insufficient Logging) | **COMPLIANT** (remediated 2026-09-15) | HIGH (closed) | V96 adds `applicant_employee_id`/`sanctioned_by_employee_id`/`disbursed_by_employee_id`/`rejected_by_employee_id` to `cpf_loan_applications`, populated on every transition; `CpfLoanApplication.auditSnapshot()` updated to include them so the existing generic `AuditLog`/`AuditableEntityListener` infrastructure (already wired via `@EntityListeners`, previously undiscovered by the original audit pass) also captures them. Additionally fixed `AuditActor.currentUsername()`, which previously trusted a client-spoofable `X-Acting-User` header (a pre-auth-era placeholder) — it now resolves the real Spring Security principal, making the existing generic audit trail trustworthy for every audited entity, not just CPF. | Was: no forensic reconstruction possible; audit "actor" was spoofable. Now: business-meaningful actor columns on the entity itself, plus a trustworthy generic audit trail. | Historical rows remain unattributed (not backfilled, by design — no actor identity exists to fabricate). | — (closed) | `CpfLoanApplicationServiceTest` (2 new persistence tests), `AuditActorTest` (rewritten, 5 tests) |
| SEC-009 | Financial / Concurrency | Duplicate payroll-batch creation under concurrency must return a controlled conflict, not a raw 500 | CWE-362 (Race Condition / TOCTOU) | PARTIAL | MEDIUM | `PayrollBatchService.create():38-42` check-then-insert with no pessimistic lock; `GlobalExceptionHandler.java` has no handler for `DataIntegrityViolationException` or a generic fallback | DB unique constraint (`uq_payroll_batch_month_year`) prevents actual duplication (integrity preserved); concurrent loser gets an unhandled 500 instead of the same 409 the sequential path returns | Add a `DataIntegrityViolationException` handler mapping the unique-constraint violation to the existing `MasterDataConflictException`/409 shape | HARDEN_BEFORE_FORMAL_VAPT | Concurrency test: two simultaneous `create()` calls for the same month/year → both return 409, none return 500 |
| SEC-010 | Authorization | Technical administration must not automatically imply financial approval authority | OWASP A01:2021 (Broken Access Control) | **COMPLIANT — financial authorization boundary. Legacy non-financial RBAC migration remains open.** | MEDIUM | New DB-backed model: `role_permissions` (V94 seed) grants `SYSTEM_ADMIN` only `USER_*`/`AUDIT_VIEW`/`SECURITY_DIAGNOSTICS_VIEW`/`EMPLOYEE_VIEW` — zero `CPF_*`/`PAYROLL_*`/`DISBURSEMENT_*`/`JCIECCS_*` permissions, verified by `RbacSecurityTest`. **2026-09-16 financial closure**: every endpoint matching the named critical categories (CPF/Payroll-finalization/JCIECCS approval — 10 controllers, ~19 endpoints) had `SUPER_ADMIN` removed. **2026-09-16 non-financial migration (same day, second pass)**: full `SUPER_ADMIN` inventory across all 87 controller files (232 occurrences after the financial pass) closed to **28 remaining** — 26 are class-level/maker/read grants on already-verified financial controllers (unchanged, re-confirmed not a checker bypass), 2 are `StateMasterController`/`DistrictMasterController`'s sole `hasRole('SUPER_ADMIN')` (no replacement role identified, left as REQUIRES_BUSINESS_CONFIRMATION rather than guessed). The three previously-deferred items are now implemented: `JciEccsMemberController.changeStatus` (SUPER_ADMIN removed, COOP_ADMIN retained + new `JCIECCS_APPROVE` permission path added), `TerminalSettlementController.approve` and `PayrollMasterController`'s 3 rate-edit endpoints (SUPER_ADMIN removed, existing HR_ADMIN/FINANCE_ADMIN retained, exact final checker role still REQUIRES_BUSINESS_CONFIRMATION). A supplemental financial gap was also found and closed: `LoanController`/`PfLedgerController` (a general employee-loan module missed by the original SEC-003/004 pass) had `disburse`/`foreclose`/`settle` reachable by SUPER_ADMIN alone — narrowed to the same pattern. The shared SEC-002/005/006/007 ownership helper (`AttendanceAggregationSecurity.canActOnBehalfOfOthers`) also had a hardcoded SUPER_ADMIN grant, now removed (HR_ADMIN only). See `RBAC_MIGRATION_REPORT.md` for full detail. | New RBAC-permission-gated endpoints (onboarding, role assignment, and now JCIECCS membership status) are correctly boundary-safe. **Zero unexplained financial bypass remains** — every one of the 28 remaining SUPER_ADMIN occurrences is either a verified financial read/maker grant or an explicitly-flagged REQUIRES_BUSINESS_CONFIRMATION item, none silently guessed. | Resolve the 3 remaining REQUIRES_BUSINESS_CONFIRMATION items (terminal settlement checker role, payroll master-data edit ownership, state/district master ownership). Seed permissions for `HR_ADMIN_PERS`/`HR_ADMIN_EST`/`IT_ADMIN_STORE` only once a live endpoint and a real business specification exist for them — not invented ahead of need. | INCORPORATE_IN_RBAC (continued — 3 named items + reserved-role permission seeding remain) | `RbacSecurityTest`, `CpfLoanControllerSecurityTest`/`PayrollRunControllerTest` (financial closure), `JciEccsMemberControllerSecurityTest`/`TerminalSettlementControllerSecurityTest`/`PayrollMasterControllerTest`/`LoanControllerTest`/`PfLedgerControllerTest`/`AttendanceAggregationSecurityTest` (non-financial closure, this pass) |
| SEC-011 | Authorization | Consistent authorization posture for equivalent master data | OWASP A01:2021 | NON_COMPLIANT | LOW | `MasterStateController` (`isAuthenticated()`) vs `StateMasterController` (`hasRole('SUPER_ADMIN')`) — same conceptual data, different gates | Minor inconsistency, low-sensitivity data | Reconcile to one authorization posture | FIX_BEFORE_FORMAL_VAPT | Code review |
| SEC-012 | Input Validation / Export | XLSX export must neutralize formula-injection payloads | OWASP A03:2021 (Injection, adjacent), CWE-1236 (Improper Neutralization of Formula Elements in a CSV File) | NON_COMPLIANT | MEDIUM | `XlsxExportService.java:97-103` writes free-text DB values into cells via `setCellValue()` with no `=+-@` prefix-escaping | A malicious value stored via any free-text field (name, remarks) could execute as a formula when the exported file is opened in some spreadsheet clients | Prefix-escape cell values starting with `=`,`+`,`-`,`@` before `setCellValue()` | FIX_BEFORE_FORMAL_VAPT | Unit test: export a row containing `=1+1` → assert stored cell value is escaped |
| SEC-013 | SQL Injection (hardening) | Reporting queries should use parameterized array binding for ID lists | CWE-89 (adjacent, not currently exploitable) | PARTIAL | LOW | `AlmsReportService.java` (9 sites) concatenates a Java-built numeric ID list into `IN (...)` SQL text; values are DB-sourced longs today, never raw request text | Not currently exploitable; relies on Java-side type safety rather than JDBC parameterization as the security boundary | Migrate to `= ANY(?)` array binding | HARDEN_BEFORE_FORMAL_VAPT | Code review post-refactor |
| SEC-014 | File Security | Document-download endpoint (when built) must enforce object-level authorization from day one | OWASP API1:2023 | NOT_APPLICABLE (feature not built) | INFORMATIONAL | `DocumentStorageService` interface exposes only `store()`; no retrieval endpoint exists; no ownership tracking (`entityRefId`) is persisted anywhere yet | N/A today; live risk the moment a download endpoint is added without ownership checks | Design ownership tracking into the storage/upload model now, before a download endpoint is built | INCORPORATE_IN_RBAC | Design review at implementation time |
| SEC-015 | Rate Limiting | Sensitive/expensive/auth-adjacent endpoints should have rate limiting | OWASP API4:2023 (Unrestricted Resource Consumption) | NON_COMPLIANT (app layer) / REQUIRES_INFRA_VERIFICATION (proxy layer) | MEDIUM | No bucket4j/resilience4j/custom throttling found anywhere in `backend/pom.xml` or source | Brute-force/abuse exposure, especially relevant to future onboarding activation/resend endpoints | Add rate limiting at gateway/WAF or application filter level, prioritizing onboarding activation/resend | INCORPORATE_IN_RBAC (onboarding) / HARDEN_BEFORE_FORMAL_VAPT (general) | Load/abuse test once implemented |
| SEC-016 | Token Security | JWT storage should minimize XSS-exfiltration exposure | OWASP A03:2021 (adjacent), CWE-522 (Insufficiently Protected Credentials) | PARTIAL | LOW/INFORMATIONAL | JWT stored in `localStorage` (`AuthContext.tsx:16,20,30,35`; `client.ts:4,21,51`), not httpOnly cookie | Standard SPA tradeoff; any future XSS could exfiltrate the token — no CSP yet to mitigate | Short token TTL, define CSP (see SEC-018), avoid any future raw-HTML rendering | HARDEN_BEFORE_FORMAL_VAPT | Token TTL review + CSP rollout |
| SEC-017 | Error Handling | Uncaught exceptions should map to controlled responses, not framework defaults | CWE-755 (Improper Handling of Exceptional Conditions) | PARTIAL | INFORMATIONAL | `GlobalExceptionHandler.java` covers ~13 specific business exceptions; no catch-all `@ExceptionHandler(Exception.class)` — falls through to Spring Boot's safe (non-leaking) default `/error` handler | Not a leak (Boot defaults are safe), but a reliability/observability gap; ties directly to SEC-009 | Add a sanitized catch-all handler | HARDEN_BEFORE_FORMAL_VAPT | Code review |
| SEC-018 | Security Headers | CSP / Referrer-Policy / Permissions-Policy / HSTS should be defined | OWASP A05:2021 (Security Misconfiguration) | PARTIAL / REQUIRES_INFRA_VERIFICATION | MEDIUM | `SecurityConfig.java` never calls `.headers(...)` — Spring Security defaults apply (`X-Frame-Options: DENY`, `nosniff`); no CSP/Referrer-Policy/Permissions-Policy set anywhere; HSTS conditional on HTTPS (ALB is HTTP-only today) | No content-injection defense-in-depth beyond default framing/sniffing protection | Define a staged CSP; confirm whether any future reverse proxy/CDN adds the rest | FIX_BEFORE_PRODUCTION (app-settable headers) / REQUIRES_INFRA_VERIFICATION (proxy-settable headers) | Header scan against a running instance |
| SEC-019 | Supply Chain / CI-CD | Third-party GitHub Actions should be pinned to immutable references | CWE-829 (Inclusion of Functionality from Untrusted Control Sphere) | NON_COMPLIANT | MEDIUM | `.github/workflows/ci-cd.yml` pins `actions/checkout@v4` etc. to floating major-version tags, not commit SHAs, in a job holding `id-token: write` that assumes an AWS deploy role | A compromised/retagged upstream action could run with deploy-role assumption capability | Pin actions to commit SHAs; use Dependabot/Renovate to bump | HARDEN_BEFORE_FORMAL_VAPT | CI config review |
| SEC-020 | Dependency Security | Backend/frontend dependencies should be scanned for known vulnerabilities in CI | OWASP A06:2021 (Vulnerable & Outdated Components) | NON_COMPLIANT | MEDIUM | No `dependency-check-maven`/OWASP plugin in `backend/pom.xml`; no `npm audit` step in `.github/workflows/ci-cd.yml` (manual `npm audit --omit=dev` run during this audit returned 0 vulnerabilities at time of check, but this isn't wired into CI) | Vulnerable dependencies could ship undetected | Add SCA scanning (OWASP Dependency-Check or Trivy/Grype) to CI | FIX_BEFORE_FORMAL_VAPT | CI pipeline review post-change |
| SEC-021 | CI/CD / Cloud IAM | OIDC deploy-role trust policy should be scoped to the minimum ref/branch needed | OWASP A01:2021 / CWE-284 (Improper Access Control) | PARTIAL | LOW/MEDIUM | `infra/github-actions-deploy-role/trust-policy.json` — `sub` StringLike `repo:saumyadeep23/jci-hrms:*` (any ref in the repo, not just `push` to `main`); also contains one dead/non-matching `sub` pattern (`repo:...@*/...@*:*`, doesn't match GitHub's actual OIDC claim grammar — inert, cleanup only) | Broader trust grant than the single current job needs; moot today (one workflow, correctly `if`-gated) but a risk once more workflows exist | Scope `sub` to `repo:saumyadeep23/jci-hrms:ref:refs/heads/main`; remove the dead pattern | HARDEN_BEFORE_FORMAL_VAPT | Trust-policy review |
| SEC-022 | Supply Chain | Docker base images should be pinned by digest, not tag | CWE-829 | PARTIAL | LOW | `backend/Dockerfile:2,10` — `maven:3.9-eclipse-temurin-21`, `eclipse-temurin:21-jre-jammy` (tag-pinned, not digest-pinned) | Floating within the tag as upstream pushes new images | Pin by `@sha256:` digest | HARDEN_BEFORE_FORMAL_VAPT | Dockerfile review |
| SEC-023 | Secure Configuration | CORS origin list must be environment/profile-driven, not hardcoded dev/LAN patterns | OWASP A05:2021 | PARTIAL | LOW | `SecurityConfig.corsConfigurationSource()` hardcodes localhost/127.0.0.1/LAN-wildcard origins directly in `main` source, no profile gate, and **no production origin is configured anywhere yet** | Not independently exploitable (attacker can't set an already-excluded Origin), but dead attack-surface noise in a production build and no real frontend origin is wired | Move CORS origins to env/profile-driven config; add the real production origin when known; drop dev/LAN entries from the production profile | FIX_BEFORE_PRODUCTION | Config review + CORS test against a "production" profile |
| SEC-024 | Secrets / Local Dev | Terraform local state should not hold plaintext secrets long-term | CWE-312 (Cleartext Storage of Sensitive Information) | REQUIRES_INFRA_VERIFICATION | INFORMATIONAL | `infra/terraform.tfstate` (gitignored, confirmed not tracked) holds resource attributes including the real DB password, on local disk | Local-machine-only exposure today; not a repo/git leak | Complete the already-stubbed S3+DynamoDB backend migration (`infra/versions.tf`) with SSE-KMS enabled | INFRA_VERIFICATION_REQUIRED | Confirm backend migration + encryption once implemented |
| SEC-025 | Secure Configuration | Actuator, DB credentials, IAM, encryption-at-rest, RDS/S3 hardening gates | Various | COMPLIANT | — | Verified independently (not just trusted from CLAUDE.md): actuator minimal exposure; Secrets Manager for DB password in every environment; IAM roles genuinely least-privilege with correct `PassRole` condition-gating; RDS/S3 encryption + public-access-block; WAF/Multi-AZ hardening gates fire correctly on `local.hardened`; security-group chain confirmed; ECR immutable tags + scan-on-push; `npm audit` clean; no committed real secrets; no H2 console/mock-auth/test-controller leakage into `main`; no SQLi/XSS/deserialization/command-exec/SSTI findings; file-upload pipeline (MIME+extension allow-list, magic-byte validation, path-traversal double-guard) genuinely well designed; SSRF-safe outbound lookup proxies (fixed base URL + strict input regex + URI-template binding) | — | Maintain these controls; no action needed | — | Re-verify at next audit cycle |
| SEC-026 | Infrastructure / TLS | ALB HTTPS listener / TLS termination | OWASP A02:2021 (Cryptographic Failures, when deployed) | REQUIRES_INFRA_VERIFICATION | — (deliberate, documented gap per CLAUDE.md) | `infra/main.tf:386-453` — HTTPS listener only exists when `var.acm_certificate_arn` is set; currently unset (`dev` only, no domain/cert provisioned) | No confidentiality/integrity protection in transit until a cert is wired | Provision ACM cert + set `acm_certificate_arn` before any staging/prod rollout — do not treat as drive-by cleanup for `dev` per CLAUDE.md's explicit instruction | INFRA_VERIFICATION_REQUIRED (before staging/prod) | Confirm HTTPS listener active + `ssl_policy` pinned once a cert exists |
| SEC-027 | PII / Field-Level Security | List-view DTOs should not over-expose PAN/Aadhaar/bank/salary fields | OWASP A01:2021 / API3:2023 (Broken Object Property-Level Authorization) | REQUIRES_BUSINESS_CONFIRMATION | MEDIUM (pending verification) | `EmployeeController.list()` returns `Page<EmployeeResponse>` (DTO, not raw entity) — field-by-field masking on the list view vs. single-record view was not exhaustively diffed in this pass | Possible over-exposure of sensitive fields to any role with list access | Confirm field-level minimization per role during RBAC field-level-security design (spec §71) | INCORPORATE_IN_RBAC | DTO field audit during RBAC design |

**Positive/COMPLIANT controls not listed individually above but confirmed with evidence in `PRE_RBAC_SECURITY_AUDIT.md`:** stateless session model (N/A for session-fixation controls), consistent Bean Validation coverage (153/166 request DTOs), zero mass-assignment paths (138 request-body types vs. 289 entities, no overlap), zero SQL injection via native/JPQL, zero XSS sinks in frontend, zero deserialization/command-execution/SSTI findings, JCIECCS Phase 5 idempotency-key + pessimistic-lock + fixed-lock-ordering concurrency architecture, `@jciEccsSec.isSelf`/`EmployeeSecurity`/`LoanSecurity`/etc. object-level authorization pattern correctly applied in the large majority of endpoints, 47 existing backend test files already covering 401/403/`@WithMockUser` scenarios.
