# Requirement Traceability Matrix — PIMS & ALMS Modules

Traces every FR in `SRS_PIMS_ALMS.md` to its real DB tables, backend entities/services, API endpoints, and frontend views. `Status` reflects actual code state, not intent: **Implemented** (fully wired end-to-end), **Partial** (schema/backend exists but a piece is missing or unwired), **Schema Only** (table exists, nothing reads/writes it).

| Req ID | Business Requirement | Category | DB Tables / Constraints | Backend Entities & Services | API Endpoints | Frontend Views / Modals | Status |
|---|---|---|---|---|---|---|---|
| FR-PIMS-001 | Employee Master 360 Profile | PIMS Core | `vw_jci_employee_master_360` (view, V32/V57/V60) joining `employees`, `employee_employment_categories`, `grade_scale_master`, etc. | `Employee360Service`, `EmployeeController`; PIMS aggregate reports separately via `PimsReportController` (`/api/v1/reports/pims/cadre-strength`, `.../superannuation`, `.../reservation-roster`, `.../manpower-4tier`, `.../apar-matrix`, `.../ad-hoc`, `.../export`) | `GET /api/employees/{id}/360` | Employee 360 profile drawer, PIMS reporting hub tabs (`frontend/src/pages/reports/pims/`) | Implemented |
| FR-PIMS-002 | Employee Onboarding Wizard (8 steps) | PIMS Core | `onboarding_drafts` (V25), `employees` and related step tables | `EmployeeOnboardingService`, `EmployeeOnboardingController` | `POST/PUT /api/v1/onboarding/...` | `OnboardingWizardPage.tsx`, `Step1Personal.tsx`…`Step8Review.tsx` | Implemented |
| FR-PIMS-003 | Post Master & Cadre/Designation Structure | PIMS Core | `post_master` (V6, FK `department_id`→`departments`, `designation_id`→`designations`, self-FK `operational_reporting_post_id`/`administrative_reporting_post_id`), unique `uq_post_master_post_code_active` | `PostMaster` entity, `PostMasterService`, `PostMasterController` | `POST /api/v1/posts`, `GET .../{id}`, `GET`, `GET .../vacant`, `.../available-for-assignment`, `.../summary`, `.../{id}/incumbency-history`, `POST .../{id}/incumbency` | `pages/admin` post/designation masters | Implemented |
| FR-PIMS-004 | Post Incumbency Assignment | PIMS Core | `post_incumbency` (V6, FK `post_id`, `employee_id`; `ck_post_incumbency_assignment_type`; partial unique `uq_post_incumbency_post_substantive_active`) | `PostIncumbency` entity, `PostIncumbencyService`, `PostIncumbencyController` | `POST /api/post-incumbencies`, `GET .../{id}`, `GET`, `POST .../{id}/end` | Post incumbency admin views | Implemented |
| FR-PIMS-005 | Grade Scale Master & Pay Fixation (post-V60) | PIMS Core | `grade_scale_master` (V48), `regular_pay_fixations` (V50, FK `employee_id`, `scale_code`→`grade_scale_master.scale_code`, `is_current` flag); `pay_scale_master` deprecated, `employees.pay_scale_id`/`employee_employment_categories.pay_scale_id` **dropped** (V60) | `GradeScaleMaster`, `RegularPayFixation` entities; `RegularPayFixationRepository.findByEmployeeIdAndCurrentTrue`; `GradeScaleController` (live), `PayScaleController`/`PayScaleService` (`@Deprecated`, read-only) | `GET/POST /api/v1/masters/grade-scales`; `GET /api/pay-scales` (read-only) | `pages/admin/master/GradeScalesTab.tsx`, `Step6Employment.tsx` | Implemented |
| FR-PIMS-006 | Increment Processing (3% IDA) | PIMS Core | Reads `employee_employment_categories`, `regular_pay_fixations`, `grade_scale_master`, `disciplinary_cases` (raw SQL, no dedicated table) | `IncrementProcessingService`, `IncrementController` | `GET /api/v1/increments/due-list`, `POST /api/v1/increments/process-batch` | Increment processing admin view | Implemented |
| FR-PIMS-007 | Movement Orders & Digital Service Book | PIMS Core | `employee_service_book` (V12, repointed `grade_scale_id` by V60), movement-lifecycle tables (V46) | `MovementOrderService`, `EmployeeServiceBookService`/`EmployeeServiceBook` entity, `MovementLifecycleController` | `GET /api/service-book/{employeeId}/timeline`, movement-order endpoints via `MovementLifecycleController` | `ServiceBookTab.tsx` | Implemented |
| FR-PIMS-008 | Superannuation Date Auto-Calculation | Separation & Settlement | `employee_superannuation_details` (V31), trigger `trg_calc_jci_superannuation`/`fn_calculate_jci_superannuation_date()`, procedure `sp_apply_director_ministry_extension` | DB trigger (no Java service — pure PL/pgSQL) | N/A (fires on `employees` DOB write) | Reflected read-only in `SuperannuationTab.tsx` | Implemented |
| FR-PIMS-009 | Daily Superannuation Sweep & Auto-Release | Separation & Settlement | `employee_superannuation_details`, `employees.status` | `SuperannuationScheduledTask` (`@Scheduled` cron `0 5 0 * * ?`), `EmployeeReleaseService` | N/A (scheduled job) | — | Implemented |
| FR-PIMS-010 | Automated Exit Dossier Initiation (T-90) | Separation & Settlement | `exit_clearance_requests`, `employee_superannuation_details` | `ExitInitiationScheduler` (`@Scheduled` cron `0 30 0 * * ?`, `LOOKAHEAD_DAYS=90`) | N/A (scheduled job) | — | Implemented |
| FR-PIMS-011 | Multi-Department Exit Clearance | Separation & Settlement | `exit_clearance_requests` (V58, `ck_...status`), `exit_clearance_items` (`ck_...department_code`, `ck_...status`) | `ExitClearanceService`, `ExitClearanceController` | `POST /api/v1/exit-clearances`, `GET .../{id}`, `GET .../by-employee/{employeeId}`, `GET .../{id}/checklist`, `PUT .../items/{itemId}`, `POST .../{id}/finalize` | `ExitClearanceModal.tsx` | Implemented |
| FR-PIMS-012 | Terminal Settlement Computation (Gratuity/CPF/EL-HPL encashment, DoPT Rule 39) | Separation & Settlement | `terminal_settlements` (V58) | `TerminalSettlementService` | `GET /api/v1/settlements/preview/{employeeId}`, `POST .../generate/{employeeId}`, `GET .../{id}`, `POST .../{id}/approve` | `TerminalSettlementModal.tsx` | Implemented |
| FR-PIMS-013 | Beneficiary Disbursement for Deceased Cases | Separation & Settlement | `terminal_settlement_beneficiaries` (V58, nullable bank fields per V59), `employee_nominees` (V5, auto-source) | `TerminalSettlementService` (nominee auto-populate + approve()-time bank-detail enforcement) | `POST /api/v1/settlements/{id}/beneficiaries` | `TerminalSettlementModal.tsx` (beneficiary section) | Implemented |
| FR-PIMS-014 | Separated Employee Directory | Separation & Settlement | `employees` (status-filtered) | `SeparatedEmployeeDirectoryService` | `GET /api/v1/...separated-employees...` | `SeparatedStaffTab.tsx` | Implemented |
| FR-PIMS-015 | Idempotent Sequential Code Generation | PIMS Core (cross-cutting) | `employees` (advisory-lock function), `cpf_ac_no` column (V56) | `CpfAcNoGeneratorService`, `EmployeeCodeGeneratorService` | N/A (internal, invoked during onboarding) | — | Implemented |
| FR-ALMS-001 | Leave Type Master & Cadre Eligibility | ALMS & Leave Management | `leave_types` (V7, seeded V15/V16), `leave_type_cadre_eligibility` (V37) | `LeaveTypeService`, `LeaveTypeController`/`LeaveTypeMasterController` | `GET/POST /api/leave-types` (and master variant) | `LeaveTypeMasterPage.tsx` | Partial — seed figures explicitly flagged as unverified placeholders pending HR review |
| FR-ALMS-002 | Leave Application Submission & Approval | ALMS & Leave Management | `leave_applications` (V7/V14/V36; `ck_...status`; GiST exclusion constraints) | `LeaveApplicationService`, `LeaveApplicationController` | `POST /api/leave-applications`, `.../preview`, `GET .../{id}`, `PUT .../{id}`, `GET`, `POST .../{id}/submit`, `.../{id}/approve`, `.../{id}/reject`, `.../{id}/cancel` | `LeaveApplicationForm.tsx`, `LeavePage.tsx`, `LeaveSanctionQueuePage.tsx` | Partial — single-tier approval only; no `RECOMMENDED`/multi-tier state exists despite "multi-tier approval" being a common expectation for this workflow shape |
| FR-ALMS-003 | Combined CL + RH Application | ALMS & Leave Management | `leave_applications.group_application_id`/`rh_entry_id` (V36) | `CombinedLeaveApplicationService`, `CombinedLeaveController` | `POST /api/leave-applications/combined` | `CombinedLeaveApplicationForm.tsx` | Implemented |
| FR-ALMS-004 | Leave Ledger (Append-Only) | ALMS & Leave Management | `leave_ledger_entries` (V14, `ck_leave_ledger_entries_source` widened by V59) | `LeaveLedgerEntryService`, `LeaveLedgerEntryController` | `GET /api/leave-ledger-entries` (read) | Ledger view within leave/ESS pages | Implemented |
| FR-ALMS-005 | Leave Entitlement Sub-Ledger (EL split) | ALMS & Leave Management | `leave_entitlement_balance` (V36; `ck_...credited_split`) | `LeaveEntitlementBalanceController` (read), populated by `ElAccrualService`/leave workflows | `GET /api/leave-entitlement-balances` | `LeaveBalanceSplitCard.tsx` | Implemented |
| FR-ALMS-006 | Semi-Annual EL Accrual | ALMS & Leave Management | `leave_entitlement_balance`, `leave_ledger_entries` (source `EL_SEMI_ANNUAL_ACCRUAL`) | `ElAccrualService` (`@Scheduled` cron `0 0 0 1 1,7 *`) | N/A (scheduled job) | — | Partial — EOL 1/10th deduction is a documented approximation (proxied from LWP debits), not an exact CCS(Leave) Rules EOL implementation |
| FR-ALMS-007 | Leave Baseline Take-On | ALMS & Leave Management | `leave_baseline_initialization` (V36; `ck_...el_split`) | `LeaveBaselineTakeOnService`, `LeaveBaselineTakeOnController` | `POST /api/v1/admin/leave/baseline-takeon` | `LeaveBaselineTakeOnPage.tsx` | Implemented |
| FR-ALMS-008 | Leave Encashment (dual HR+Finance approval) | ALMS & Leave Management | `leave_encashment_application` (V36; `ck_..._in_service_el_rule`, `ck_..._dual_approval`) | `LeaveEncashmentService`, `LeaveEncashmentController` | `POST /api/v1/self-service/leave/encashment`, `GET .../mine`, `GET /api/v1/admin/leave/encashment` | `LeaveEncashmentPage.tsx` | Implemented |
| FR-ALMS-009 | Shift Master & Scheduling | ALMS & Leave Management | `shift_master` (V43), `employee_shift_schedule`/DPC shift defaults (V44/V45) | `ShiftMasterService`, `ShiftResolutionService`, `ShiftMasterController` | `POST /api/v1/attendance/shifts`, `GET .../{id}`, `GET`, `GET .../{id}/dependencies` | `ShiftsMasterTab.tsx`, `DutyRosterPage.tsx` | Implemented |
| FR-ALMS-010 | Biometric/Mobile Punch Ingestion | ALMS & Leave Management | `mobile_punches` (V7), device registration (V38) | `MobilePunchController`, `AttendanceAggregationService` (consumer) | `POST /api/attendance/punch`, `GET .../{id}`, `GET` | `AttendancePunchPage.tsx`, `CameraCapture.tsx`, `RegisteredDevicesCard.tsx` | Implemented |
| FR-ALMS-011 | Daily Attendance Aggregation | ALMS & Leave Management | `daily_attendance` (V7/V14/V36; `ck_daily_attendance_status`, `ck_daily_attendance_detail_status`), `office_timing_config` (V36) | `AttendanceAggregationService`, `AttendanceAggregationController` | `POST /api/attendance/aggregation/evaluate`, `.../evaluate-day` | `AttendanceDetailTable.tsx`, `AttendanceHistoryController`-backed views | Implemented |
| FR-ALMS-012 | Attendance Regularization Workflow | ALMS & Leave Management | `attendance_regularization_applications` (V36; `ck_...status`, `ck_...reason_code`) | `AttendanceRegularizationService`, `AttendanceRegularizationController` | `POST /api/v1/attendance/regularization`, `GET .../mine`, `GET .../pending-approval` | `RegularizationModal.tsx`, `RegularizationApprovalQueuePage.tsx` | Implemented |
| FR-ALMS-013 | Attendance-Linked Leave Deduction | ALMS & Leave Management | `leave_ledger_entries` (sources `AUTO_LATE_DEDUCTION`, `COMMUTED_LEAVE_HPL_DEBIT`) | `AttendanceLeaveDeductionService` | N/A (invoked from aggregation pipeline) | — | Implemented |
| FR-ALMS-014 | Holiday Calendar Master | ALMS & Leave Management | `holidays` (V14, multi-state V39; `ck_holidays_holiday_type`) | `HolidayMasterService`/`HolidayService`, `HolidayMasterController`/`HolidayController` | `POST /api/holidays`, `GET .../{id}`, `GET`, `GET .../my-calendar` | `HolidayMasterPage.tsx`, `HolidayCalendarCard.tsx` | Implemented |
| FR-ALMS-015 | Attendance Payroll Cutoff Freeze | ALMS & Leave Management | `attendance_payroll_cutoff` (V36; `uq_...month_year`) | `AttendancePayrollCutoff` entity exists; **no service writes to this table** | None found | — | Schema Only — not wired to any writer; `PayrollComputationService` derives its cutoff window independently instead |
| FR-ALMS-016 | LOP Computation for Payroll | ALMS & Leave Management (cross-cutting into Payroll) | `daily_attendance` (status `ABSENT`/`HALF_DAY`) | `PayrollComputationService.computeLopDays()` | N/A (internal, invoked during payroll run compute) | — | Partial — a day with no `daily_attendance` row is not counted as LOP (no daily batch populates one row per employee per working day yet) |
